import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Favs = () => {
  return (
    <View>
      <Text>Favs</Text>
    </View>
  );
};

export default Favs;

const styles = StyleSheet.create({});
